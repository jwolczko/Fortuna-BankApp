meta {
  name: Get Dashboard
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/dashboard/{{customerId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("dashboard returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
